** Steps to Run this perticular code on your pc **
  Make sure you installed anaconda in tour pc and add it to environmental variables 

Lets began

open the directory andcode prompt by "cd" command and type "code ." to open VS Code

crate . env and store your api key in that<i>(Make sure you dont share it with any one)</i>

ADD SPECIFIC LIBRARY IN REQUIREMENT which are needed

Create environment using "conda create -p venv python==3.10"

Activate envirinment using "conda activate venv/"pi

Now just run app.py in streamlit using "streamlit run app.py" in new terminal

